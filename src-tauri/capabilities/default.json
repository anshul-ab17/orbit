{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the dashboard, webcam bubble, and ripple overlay",
  "windows": ["main", "webcam", "ripple"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:event:default",
    "opener:default",
    "global-shortcut:default"
  ]
}
